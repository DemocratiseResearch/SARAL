info:
  name: User - Get API Keys
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/api/user/keys
  headers:
    - name: X-User-ID
      value: testuser1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
