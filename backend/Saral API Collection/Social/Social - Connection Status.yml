info:
  name: Social - Connection Status
  type: http
  seq: 96

http:
  method: GET
  url: http://localhost:8080/api/social/status
  headers:
    - name: X-User-ID
      value: testuser1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
