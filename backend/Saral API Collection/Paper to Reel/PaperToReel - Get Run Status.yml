info:
  name: PaperToReel - Get Run Status
  type: http
  seq: 65

http:
  method: GET
  url: http://localhost:8080/api/papertoreel/:run_id/status
  headers:
    - name: X-User-ID
      value: testuser1
  params:
    - name: run_id
      value: your-run-id-here
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
