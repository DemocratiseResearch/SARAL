info:
  name: PaperToReel - Finalize
  type: http
  seq: 71

http:
  method: POST
  url: http://localhost:8080/api/papertoreel/:run_id/finalize
  headers:
    - name: X-User-ID
      value: testuser1
  params:
    - name: run_id
      value: your-run-id-here
      type: path
  body:
    type: json
    data: |-
      {
        "output_format": "mp4"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
