info:
  name: PaperToVideo - Generate Video
  type: http
  seq: 26

http:
  method: POST
  url: http://localhost:8080/api/papertovideo/:run_id/generate-video
  headers:
    - name: X-User-ID
      value: testuser1
  params:
    - name: run_id
      value: your-run-id-here
      type: path
  body:
    type: json
    data: |-
      {
        "output_format": "mp4",
        "voice_gender": "male",
        "language": "english"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
