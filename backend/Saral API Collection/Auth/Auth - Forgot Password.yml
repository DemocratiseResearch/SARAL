info:
  name: Auth - Forgot Password
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/auth/email/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "user@example.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
