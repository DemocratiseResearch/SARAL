info:
  name: Auth - Verify Token
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8080/api/auth/verify
  headers:
    - name: X-User-ID
      value: testuser1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
