info:
  name: PaperToPoster - Confirm
  type: http
  seq: 53

http:
  method: POST
  url: http://localhost:8080/api/papertoposter/:run_id/confirm
  headers:
    - name: X-User-ID
      value: testuser1
  params:
    - name: run_id
      value: your-run-id-here
      type: path
  body:
    type: json
    data: |-
      {
        "language": "english"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
