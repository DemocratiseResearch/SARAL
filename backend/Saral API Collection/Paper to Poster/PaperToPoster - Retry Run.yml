info:
  name: PaperToPoster - Retry Run
  type: http
  seq: 55

http:
  method: POST
  url: http://localhost:8080/api/papertoposter/:run_id/retry
  headers:
    - name: X-User-ID
      value: testuser1
  params:
    - name: run_id
      value: your-run-id-here
      type: path
  body:
    type: json
    data: |-
      {
        "from_step": "poster_gen"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
